spring:
  application:
    name: service-hi
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8887/
server:
  port: 8083
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8086/eureka/